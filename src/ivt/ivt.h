#ifndef __PORT_H
#define __PORT_H
#include "../types.h"
// define header and include the types header

// this header file will declare the idt table, that will be written using a class, with each object being an entry in it.
// each entry is the definition of an interrupt




#endif
